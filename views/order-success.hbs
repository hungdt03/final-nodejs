<div class="flex items-center justify-center h-full p-8">
    <div class="flex flex-col items-center justify-center gap-y-3 bg-white p-8 w-[800px] shadow">
        <img width="100px" src="/images/system/order-success.jpg" />
        <span class="text-3xl font-semibold">Giao dịch hoàn tất</span>
        <p>Đơn hàng của bạn đã được xử lý thành công</p>
        <p>Mã đơn hàng {{order.id}}</p>
        <p>Tổng tiền: {{order.totalAmount}}</p>
        <p>Ngày giao dịch: {{order.orderDate}}</p>
        <div class="flex items-center gap-x-3">
            <a href="/" class="bg-green-600 text-white px-3 py-1 rounded-md">Trang chủ</a>
            <a target="_blank" href="/orders/invoice/{{order.id}}" class="bg-primary text-white px-3 py-1 rounded-md">Xem hóa đơn</a>
        </div>
    </div>
</div>